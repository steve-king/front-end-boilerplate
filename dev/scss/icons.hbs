/* Custom CSS template for grunticon as default does not include width and height */
{{#each customselectors}}{{this}},{{/each}}
{{prefix}}{{name}} {
	background-image: url('{{datauri}}');
	width:{{width}};
	height:{{height}};
}
